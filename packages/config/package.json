{
  "name": "@war-room/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared tooling configuration for Market War Room (TypeScript bases, etc.).",
  "files": [
    "tsconfig"
  ]
}
